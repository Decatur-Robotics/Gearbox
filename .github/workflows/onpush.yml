name: On Push

on:
  push:
    branches:
      - main
  workflow_call:
  workflow_dispatch:

jobs:
  ci:
    uses: ./.github/workflows/ci.yml

  lint:
    uses: ./.github/workflows/lint.yml

  cd:
    needs:
      - ci
      - lint
    uses: ./.github/workflows/cd.yml
